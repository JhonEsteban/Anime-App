import styled from 'styled-components';

const AnimesSection = styled.section`
  margin-top: 20px;
`;

const AnimesContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  row-gap: 20px;
  justify-content: space-between;
`;

export { AnimesSection, AnimesContainer };

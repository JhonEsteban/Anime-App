import AnimeCard from '../../components/animeCard/AnimeCard';

const Animes = () => (
  <section>
    <h1>Animes Page</h1>

    <div>
      <AnimeCard />
    </div>
  </section>
);

export default Animes;
